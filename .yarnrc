# A transitive dep of @reown/appkit (@wallet-standard/base, via Solana support we
# don't use) declares engines.node >=22. This project + Netlify build on Node 20,
# which is fine at runtime for our EVM-only flow. Ignore the engine gate so both
# local and Netlify `yarn install` succeed.
--ignore-engines true
