import { FC } from "react"

import { Header, Layout } from "~/Components"

export const TeamPage: FC = () => {
  return (
    <div>
      <Header />
      <Layout>
        Team page
      </Layout>
    </div>
  )
}
